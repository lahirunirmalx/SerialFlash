## Pull the SerialFlash component in by local path (repo root, 4 levels up).
## Replace with a registry reference once published, e.g.:
##   serial_flash: "^0.5.0"
dependencies:
  serial_flash:
    path: "../../../.."
